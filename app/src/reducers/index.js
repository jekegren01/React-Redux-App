const initialState = {

};

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        case Reducer:
            return state;
        case secondReducer:
            return state;
        default:
            return state;
    }
}